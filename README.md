# 🎨 재질 복잡도 측정기 (Material Complexity Analyzer)

재질 이미지를 업로드하면 H, C, F 세 가지 복잡도 지표를 자동으로 측정하는 웹 앱입니다.

## 📊 측정 지표

- **H (무질서도)**: 패턴이 얼마나 예측 불가능한가?
- **C (구조 복잡도)**: 패턴이 얼마나 정교한가?
- **F (경계 선명도)**: 경계가 얼마나 선명한가?

## 🚀 빠른 시작 (로컬 실행)

### 1. 설치

```bash
pip install -r requirements.txt
```

### 2. 실행

```bash
streamlit run material_complexity_app.py
```

### 3. 브라우저에서 열기

자동으로 브라우저가 열립니다 (http://localhost:8501)

## ☁️ 무료 배포 (Streamlit Cloud)

### 방법 1: GitHub 연동 (추천)

1. **GitHub 저장소 만들기**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/material-complexity.git
   git push -u origin main
   ```

2. **Streamlit Cloud 배포**
   - https://streamlit.io/cloud 접속
   - "New app" 클릭
   - GitHub 저장소 연결
   - `material_complexity_app.py` 선택
   - Deploy!

3. **링크 공유**
   - 배포 완료되면 링크 생김: `https://your-app.streamlit.app`
   - 이 링크를 후배들에게 공유

### 방법 2: 직접 업로드

1. Streamlit Cloud 접속
2. "Deploy from file" 선택
3. `material_complexity_app.py`와 `requirements.txt` 업로드
4. Deploy!

## 📱 사용 방법

1. 링크 접속 (또는 로컬 실행)
2. 재질 이미지 업로드 (JPG, PNG)
3. "측정하기" 버튼 클릭
4. 5-10초 후 결과 확인
5. 필요시 CSV 다운로드

## 🎓 후배들에게 공유하기

### 링크만 주면 끝!

```
"재질 복잡도 측정하려면 여기 들어가:
https://material-complexity.streamlit.app

사진 올리고 측정 버튼 누르면 돼.
H 값이 중요한 거야!"
```

### 또는 북마크 설명

1. 링크 복사
2. 브라우저 북마크에 저장
3. 필요할 때마다 북마크 클릭
4. 이미지 업로드 → 결과 확인

## 🔧 커스터마이징

### 색상 변경

`app.py`의 CSS 섹션 수정:

```python
.metric-card-h {
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}
```

### 지표 설명 수정

각 `st.markdown()` 섹션의 텍스트 수정

### 계산 방법 변경

`measure_complexity()` 함수 수정

## 📖 기술 정보

### 기반 논문

- Bariviera et al. (2025). "Rotation invariant patterns based on Hilbert curve." Pattern Analysis and Applications.

### 방법론

1. **이미지 전처리**: RGB → Grayscale, 512×512 리사이즈
2. **Hilbert Curve**: 2D 이미지 → 1D 시퀀스 변환
3. **Permutation Entropy**: m=5, τ=1
4. **정규화**: 0-1 범위

### 검증

- 파일럿 연구 (N=7): H와 주관 평가 일치 (r=0.685)
- H-C 상관: r=-0.943 (이론과 일치)
- C-F 상관: r=0.721

## 🐛 문제 해결

### "ModuleNotFoundError"

```bash
pip install -r requirements.txt
```

### 측정이 느려요

- 정상입니다 (512×512 계산 5-10초)
- 더 작은 이미지는 자동 리사이즈됨

### Streamlit Cloud 배포 실패

- `requirements.txt` 확인
- Python 버전 확인 (3.9+ 권장)
- GitHub 저장소 public 설정

## 📞 문의

문제가 있거나 수정이 필요하면 연락 주세요!

## 📄 라이선스

MIT License - 자유롭게 사용, 수정, 배포 가능

---

**버전**: 1.0  
**최종 업데이트**: 2025.11
